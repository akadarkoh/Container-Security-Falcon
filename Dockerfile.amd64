# Simplified Dockerfile for AMD64 with pre-built assets
FROM --platform=linux/amd64 nginx:alpine

# Copy pre-built assets
COPY dist /usr/share/nginx/html/

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
